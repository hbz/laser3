Guten Tag,

aufgrund anhaltender Inaktivität wurde Ihr Account ${user.display} (${raw(user.username)}) deaktiviert.

Bitte wenden Sie sich bei Fragen an Ihren LAS:eR-Admin oder

an den LAS:eR-Support unter laser@hbz-nrw.de

${serverURL}

<g:render template="/mailTemplates/text/signature" />